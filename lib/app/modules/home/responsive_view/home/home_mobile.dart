import 'package:flutter/material.dart';

import '../../navigators/left_side_navigator.dart';

class HomeMobile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: LeftSideNavigator());
  }
}
